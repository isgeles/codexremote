# Replace the domain, then run:
# codexremote --host 127.0.0.1 --public-url https://codex.example.com
codex.example.com {
    reverse_proxy 127.0.0.1:8787 {
        flush_interval -1
        transport http {
            response_header_timeout 180s
        }
    }
}
